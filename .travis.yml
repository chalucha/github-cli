language: d

d:
  - dmd-2.068.0
  - ldc-0.15.1
  - gdc-4.9.2

install:

script:
  - dub test --compiler=${DC}

branches:
  only:
    - master

notifications:
  email: true

sudo: false

matrix:
  exclude:
